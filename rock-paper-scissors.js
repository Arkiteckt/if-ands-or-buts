const prompt = require('prompt-sync')({sigint: true});
    let game = prompt ("You may enter: ");

    let Player1 = ("rock, paper, scissors");
    if (Player1 === ("rock"));
    if (Player2 === ("paper"));
   { console.log ("Player1 wins"); }